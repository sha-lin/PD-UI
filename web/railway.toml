[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
buildArgs = { NEXT_PUBLIC_API_URL = "https://printduka.co.ke/api/proxy" }

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
