[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
buildArgs = { NEXT_PUBLIC_API_URL = "https://print-duka-api-production.up.railway.app" }

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
